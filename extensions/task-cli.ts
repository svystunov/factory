import { ExtensionFactory } from "@mariozechner/pi-coding-agent";

export const taskCliExtension: ExtensionFactory = (pi) => {
  // Extension is loaded, pi will automatically discover skills from skills/ directory
};
